# RoboPi static Ethernet uplink: robopi-ethernet-static.service owns the
# Windows-facing interface, so NetworkManager must not run DHCP on it.
[device-robopi-ethernet-static]
match-device=interface-name:enP4p65s0
managed=0
